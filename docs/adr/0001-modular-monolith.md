# ADR-0001：第一阶段采用模块化单体

## 状态

已接受。

## 决策

FastAPI 后端采用单进程模块化单体。基础调用、AI 能力、招聘场景、审计和工作台在同一工程内，通过目录和接口边界隔离。

## 原因

- 当前只有一个上游 Key 和一个业务场景。
- 统一事务和审计比跨服务通信更重要。
- 避免复制 BEAT/OPEA 的微服务复杂度。
- 后续模块达到独立扩缩容需求时仍可拆分。
