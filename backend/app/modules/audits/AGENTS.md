# audits AGENTS.md

本模块负责业务请求审计、上游调用明细、筛选、统计和导出。

- 一次业务请求只能产生一条 `ai_request_audit`。
- 每次真实上游 HTTP 调用必须产生一条 `ai_upstream_attempt`。
- 传输重试和格式修复必须区分。
- 不保存完整请求体、提示词、简历或模型响应。
- 导出不得包含密钥、内部 Token 或敏感内容。

修改后至少验证成功、失败、重试、分页和导出路径。
